# 量化交易平台待办事项

> 更新时间：2026-07-19
> 目标：逐步建设一个具备可靠回测、AI 智能分析、风险控制和实盘接入能力的量化交易平台。

## 当前进度

### 已完成

- [x] 建立事件驱动的统一仿真内核
- [x] 将 SMA 回测迁移到统一仿真内核
- [x] 支持下一根 K 线开盘执行信号
- [x] 支持手续费和滑点
- [x] 支持成交量参与率限制
- [x] 支持部分成交、拒绝成交和成交明细
- [x] 支持止损、止盈和回测结束强制平仓
- [x] 保证同一输入下回测结果可重复
- [x] 回测 API 返回执行模型和成交记录
- [x] AI 多 Provider 基础能力
- [x] AI 技术分析和结构化决策基础能力
- [x] AI 输出解析、缓存和基础测试
- [x] 第一阶段：历史行情目录、数据质量检查与版本化回测
- [x] 第二阶段：订单生命周期、确定性撮合与交易执行模型

---

## 第一阶段：数据与回测基础设施

### 1. 历史行情数据目录

- [x] 统一行情数据模型
  - [x] `symbol`
  - [x] `timestamp`
  - [x] `open`
  - [x] `high`
  - [x] `low`
  - [x] `close`
  - [x] `volume`
  - [x] `source`
  - [x] `timeframe`
- [x] 支持 Parquet 行情文件
- [x] 使用 DuckDB 查询历史行情
- [x] 支持按交易品种、周期、时间范围查询
- [x] 统一 UTC 时间和时区转换规则
- [x] 记录数据源和导入时间
- [x] 记录数据集版本号和内容哈希

### 2. 数据质量检查

- [x] 缺失 K 线检测
- [x] 重复 K 线检测
- [x] 时间戳倒序检测
- [x] 时间间隔异常检测
- [x] OHLC 关系校验
- [x] 负价格和零价格校验
- [x] 成交量异常检测
- [x] 交易日历一致性检查
- [x] 生成数据质量报告
- [x] 数据质量不合格时阻止回测

### 3. 回测结果与数据版本绑定

- [x] 保存策略版本
- [x] 保存策略参数
- [x] 保存行情数据版本
- [x] 保存回测起止时间
- [x] 保存执行模型参数
- [x] 保存风险模型参数
- [x] 保存运行环境和模型版本
- [x] 生成回测结果哈希
- [x] 支持通过版本号复现历史回测

---

## 第二阶段：完善交易执行模型

- [x] 限价单
- [x] 市价单
- [x] 止损单
- [x] 止盈单
- [x] 追踪止损
- [x] Post-only 订单
- [x] 订单有效期
- [x] 订单主动取消
- [x] 部分成交后的订单持续管理
- [x] 订单延迟模型
- [x] 不同市场状态下的滑点模型
- [x] 盘口深度模拟
- [x] 订单队列优先级模拟
- [x] 撮合结果与真实交易所规则对齐（核心订单生命周期语义）

> 第二阶段于 2026-07-18 完成。实现位于 `app.engine.simulation`：支持
> MARKET/LIMIT/STOP_MARKET/TAKE_PROFIT_MARKET、GTC/IOC/FOK、Post-only、
> 取消与到期状态、跨 K 线部分成交续挂、附加延迟、市场状态滑点、L1 深度和
> 静态队列位置。真实交易所的逐笔撮合与历史队列重建仍需交易所规则版本和 L2/L3
> 数据；当前模型只对齐可由 K 线/L1 数据确定的核心语义，并保持完全确定性。

---

## 第三阶段：强化 AI 智能分析

### 1. 统一 AI 分析输入

- [x] 行情数据
- [x] 技术指标
- [x] 趋势状态
- [x] 波动率状态
- [x] 成交量状态
- [x] 当前持仓
- [x] 当前账户风险
- [x] 历史回测表现
- [x] 市场环境标签
- [x] 最近 AI 决策及其结果

### 2. 结构化 AI 决策协议

- [x] 统一 `decision`：买入、卖出、持有、观望
- [x] 统一 `confidence`：置信度
- [x] 统一 `regime`：市场状态
- [x] 统一 `reasons`：决策依据
- [x] 统一 `risk_factors`：风险因素
- [x] 统一 `stop_loss`
- [x] 统一 `take_profit`
- [x] 统一 `position_size`
- [x] 统一 `invalidation_conditions`
- [x] 记录 `data_timestamp`
- [x] 记录 `model_version`
- [x] 记录 `prompt_version`

### 3. AI 输出安全校验

- [x] 使用 JSON Schema 校验 AI 输出
- [x] 校验决策枚举值
- [x] 校验置信度范围
- [x] 校验止损止盈价格合理性
- [x] 校验仓位是否超过风险上限
- [x] 校验数据时间戳，防止未来数据
- [x] 防止重复下单
- [x] 置信度不足时自动降级为观望
- [x] AI 输出异常时回退到规则策略
- [x] 记录所有被拦截的 AI 决策

### 4. AI 分析解释与审计

- [x] 展示 AI 使用的数据摘要
- [x] 展示关键技术指标
- [x] 展示市场状态判断
- [x] 展示支持当前决策的因素
- [x] 展示反向风险因素
- [x] 展示推翻当前判断的条件
- [x] 保存完整 AI 输入和输出摘要
- [x] 保存 Provider、模型和耗时
- [x] 支持查看历史 AI 决策

### 5. AI 效果评估

- [x] 统计 AI 信号命中率
- [x] 统计不同置信度区间的收益
- [x] 统计信号后的最大有利变动和最大不利变动
- [x] 对比 AI 策略与规则策略
- [x] 统计 AI 输出失败率
- [x] 统计 AI 决策延迟
- [x] 统计 AI 调用成本
- [x] 对比不同模型版本表现
- [x] 支持 AI 决策复盘

---

## 第四阶段：风险管理体系

- [x] 单笔最大风险限制
- [x] 单日最大亏损限制
- [x] 最大回撤熔断
- [x] 单品种仓位限制
- [x] 单交易对集中度限制（跨交易所本地持仓聚合）
- [x] 行业或资产类别集中度限制（显式 `RISK_ASSET_GROUPS` 分组映射）
- [x] 相关性风险控制（同周期对齐收益样本门槛 + 正相关预交易闸门）
- [x] 波动率自适应仓位（ATR 相对波动率仅收紧预交易名义金额上限）
- [x] 最大杠杆限制
- [x] 连续亏损自动暂停
- [x] 禁交易时段
- [x] 黑名单品种
- [x] 组合级风险监控（本地总毛暴露）
- [x] 交易前风险检查
- [x] 交易后风险归因（确认成交增量幂等回写连续亏损、日亏与回撤）

---

## 第五阶段：策略研究与评估

- [x] 多策略组合回测
- [x] 参数网格搜索
- [x] Walk-forward 优化
- [ ] 滚动窗口回测
- [ ] Monte Carlo 模拟
- [ ] Bootstrap 稳健性测试
- [ ] 样本内 / 样本外分析
- [ ] 参数敏感性分析
- [ ] 交易成本敏感性分析
- [ ] 按市场状态分段评估
- [ ] 与买入并持有基准对比
- [ ] 与指数或行业基准对比
- [ ] 统计年化收益
- [ ] 统计夏普比率
- [ ] 统计 Sortino 比率
- [ ] 统计 Calmar 比率
- [ ] 统计最大回撤
- [ ] 统计胜率和盈亏比
- [ ] 统计 Profit Factor
- [ ] 统计换手率
- [ ] 统计交易成本占比
- [ ] 统计收益稳定性

---

## 第六阶段：可观测性与审计

- [ ] 统一结构化日志
- [ ] 记录订单完整生命周期
- [ ] 记录 AI 决策生命周期
- [ ] 记录回测实验
- [ ] 记录模型版本
- [ ] 记录策略版本
- [ ] 记录风险拦截原因
- [ ] 记录数据异常
- [ ] 增加失败任务重试
- [ ] 增加运行状态监控
- [ ] 增加 Prometheus 指标
- [ ] 增加告警规则
- [ ] 增加日志聚合
- [ ] 增加数据和结果备份

---

## 第七阶段：前端工作台

### AI 分析工作台

- [ ] 当前市场状态
- [ ] AI 当前判断
- [ ] 置信度
- [ ] 多空因素
- [ ] 风险因素
- [ ] 关键支撑阻力
- [ ] 建议仓位
- [ ] 止损止盈
- [ ] 历史判断表现

### 策略研究工作台

- [ ] 策略配置
- [ ] 参数管理
- [ ] 回测任务提交
- [ ] 回测进度
- [ ] 收益曲线
- [ ] 回撤曲线
- [ ] 交易明细
- [ ] 成交明细
- [ ] 回测结果对比
- [ ] 实验版本管理

### 风险监控面板

- [ ] 当前总仓位
- [ ] 行业或资产暴露
- [ ] 最大回撤
- [ ] 当日盈亏
- [ ] 风险限额使用率
- [ ] 被拦截交易
- [ ] 异常策略
- [ ] AI 失效状态

---

## 第八阶段：平台级能力

- [ ] 多账户管理
- [ ] 券商和交易所适配器统一
- [ ] 模拟盘
- [ ] 实盘交易
- [ ] 交易前人工确认
- [ ] 交易权限控制
- [ ] 多用户和角色权限
- [ ] 策略分享
- [ ] 策略评分
- [ ] 多租户隔离
- [ ] 任务队列
- [ ] 分布式回测
- [ ] 缓存和结果复用
- [ ] 云端部署
- [ ] 定时数据更新

---

## 本轮实施顺序

当前按以下顺序推进：

1. **限价单、止损单、延迟和盘口执行模型**
2. **AI 结构化决策协议与输出校验**
3. **AI 决策效果评估**
4. **组合风险管理**
5. **多策略回测和稳健性分析**
6. **AI 分析工作台**
7. **模拟盘与实盘适配**

## 第一阶段实施记录

- 完成日期：2026-07-18。
- 历史行情采用不可变 Parquet 数据文件与 DuckDB 元数据目录；版本号由规范化内容 SHA-256 生成，并保存数据源、导入时间、时间范围与质量报告。
- 数据质量层会检测缺失/重复/倒序/间隔/OHLC/非正价格/成交量/24x7 UTC 日历问题；不合格版本保留审计但被回测入口拒绝。
- 回测实验已持久化策略版本、参数、数据版本、实际时间范围、执行与风险模型、运行环境、结果及结果哈希，并可对版本化数据复现。
- 兼容性影响：既有 `klines` 内联回测请求继续可用；新增 `data_version` 作为互斥数据来源，目录导入和实验审计/复现为新增 API。

## 第三阶段实施记录

- 完成日期：2026-07-18。
- 新增版本化 AI 决策协议（`v4`）：统一 `buy` / `sell` / `hold` / `observe`、置信度、市场状态、依据、风险因素、止损止盈、仓位、失效条件、数据时间、模型版本与提示词版本；保留旧字段以兼容既有调用方。
- 分析输入统一保存行情、确定性技术指标、趋势/波动率/成交量状态、持仓、账户风险、交易与回测表现、市场环境和近期 AI 决策；审计事件持久化输入/输出摘要、Provider、模型、耗时及拦截原因。
- 对声明 `v4` 协议的模型输出进行 JSON Schema 风格校验，并在执行前校验枚举、置信度、仓位上限、未来时间戳、止损止盈与重复建议；低置信度建议自动降级为 `observe`。LLM 调用异常或安全拒绝时，持续运行的 LLM 策略可调用 SMA 规则策略作为回退。
- 新增历史决策、决策复盘和不可变结果记录 API；`/api/v1/ai/insights` 汇总命中率、置信度分桶收益、MFE/MAE、AI/规则对比、失败率、延迟、已知成本覆盖率和模型版本表现。后验收益指标只基于显式写入的 outcome 事件，避免在没有真实后续行情时伪造回测结果。
- 兼容性影响：原有 `decision`、`position_pct`、`invalidation_condition` 与 `/api/v1/ai/insights` 基础字段保持不变；新增协议字段、审计字段和 API 均为扩展。旧 provider 输出继续受原有止损止盈风控保护；使用 `v4` 标记时启用完整结构化协议校验。

## 无人值守 Bot 受控交易实施记录

- 完成日期：2026-07-18。
- [x] 新增 1h / 5h / 24h 已闭合 K 线多周期趋势共识；趋势不一致、弱信号或数据不足一律输出 `observe`，并记录全部分析结果。
- [x] 新增默认关闭的自动化分析与自动下单双开关、交易所/标的白名单、轮询周期、单笔和单日名义金额预算。
- [x] 自动订单必须匹配新鲜的已审计 `decision_id`，并继续通过实盘开关、Kill Switch、账户对账、全局持仓价值与 `RiskManager`；网络异常沿用幂等执行意图与对账流程。
- [x] 增加纯分析、API 状态/审计、预算原子性及同一闭合 K 线跨重启幂等下单测试；更新 `.env.example`、Bot 与 API 文档。
- 兼容性影响：现有 Telegram 命令、通用下单 API 与 Bot 配置保持兼容；新增能力默认关闭，仅通过新的 `/api/v1/bot/autopilot/*` 端点和 `BOT_AUTOPILOT_*` 配置启用。


## 第四阶段第一批实施记录

- 完成日期：2026-07-18。
- [x] 现货、合约和受控 Bot 已在创建执行意图、调用交易所前共用 `RiskManager`；覆盖单笔名义金额、日亏、回撤、频率、单品种订单名义金额、全局/单品种杠杆、Kill Switch。
- [x] 新增 SQLite 原子日名义金额预留，`MAX_DAILY_ORDER_NOTIONAL` 跨三个入口共同生效；幂等重放不会重复占用额度，交易所结果不明确时保守保留额度等待对账。
- [x] 新增连续亏损暂停、UTC 禁交易时段与黑名单标的闸门；无效请求不会消耗订单频率槽。
- [x] 风险拒绝写入 `risk_order_blocked` 审计事件，并补充 RiskManager、SQLite 原子预算、现货/合约 API 与 Bot 路径的定向测试。
- 兼容性影响：默认保留全天交易、连续亏损暂停关闭、黑名单为空；新增 `MAX_DAILY_ORDER_NOTIONAL=5000` 和 `MAX_LEVERAGE=5` 会为真实订单引入保守默认上限，可通过 `.env` 明确调整或设为 `0` 关闭对应限制。

## 第四阶段第二批实施记录

- 完成日期：2026-07-19。
- [x] 新增 `MAX_PORTFOLIO_EXPOSURE`：按本地同步持仓的绝对数量 × 市场价计算总毛名义暴露，并在现货买入、合约开仓、策略和 Bot 的统一预交易闸门中检查投影额度。
- [x] 新增 `MAX_ASSET_CONCENTRATION_PCT`：按标准化交易对跨交易所汇总本地持仓；已有组合时，拒绝会把单交易对投影毛暴露占比推过上限的新开/加仓。现货卖出、合约 reduce-only 平仓不受这两项“新增暴露”限制阻断。
- [x] 风控状态接口新增组合暴露快照与两个限额；风险拒绝继续记录 `risk_order_blocked` 审计事件，并补充快照聚合、组合限额、集中度、平仓兼容和合约 API 定向测试。
- [x] 默认值均为 `0`（关闭），保留既有部署兼容性；`.env.example` 与安全文档明确了本地持仓快照、同步/对账依赖，以及待成交订单与跨进程聚合尚未覆盖的边界。

## 第五阶段第一批实施记录

- 完成日期：2026-07-19。
- [x] 新增固定权重多策略 SMA 组合回测：同一组 K 线按策略独立执行，按 `initial_capital × weight` 隔离分配资金，并逐根 K 线聚合组合权益。
- [x] 新增 `POST /api/v1/backtest/portfolio`，返回组合指标、逐策略资金分配和完整结果；策略名称必须唯一，权重必须为正且合计为 `1.0`。
- [x] 将组合回测复用现有不可变实验记录、结果哈希和版本化数据复现流程；内联 K 线可运行并审计，但和既有单策略规则一致，只有 `data_version` 绑定的实验可通过目录复现。
- [x] 增加引擎聚合/输入校验和 API 返回/权重校验的定向测试，并更新 API 文档。
- 兼容性影响：既有 `/api/v1/backtest` 请求与响应不变；组合能力为新端点。固定权重模式不共享未使用现金、不跨策略再平衡，组合结果只反映独立策略的静态分配。


## 第五阶段第二批实施记录

- 完成日期：2026-07-19。
- [x] 新增有界 SMA 参数网格搜索：显式短/长窗口列表去重排序后生成全部有效 `short < long` 组合，最多 64 个候选。
- [x] 每个候选在相同 K 线、初始资金、执行成本和保护参数下独立回测；按总盈亏降序、回撤升序、交易数降序及窗口值稳定排序，返回最佳完整结果和紧凑候选比较指标。
- [x] 新增 `POST /api/v1/backtest/grid-search`，复用不可变实验、结果哈希和版本化数据复现；响应明确标记 `in_sample_only`，避免把参数筛选误解为样本外表现。
- [x] 增加候选去重、排序、组合上限、API 返回和输入边界的定向测试，并更新 API 文档。
- 兼容性影响：既有单策略与组合回测端点不变；网格搜索为新端点。它不会自动修改策略配置或触发下单，且只产生样本内研究结果。


## 第五阶段第三批实施记录

- 完成日期：2026-07-19。
- [x] 完善严格样本外 Walk-forward 优化：每个滚动训练窗独立选择 SMA 候选，并仅将紧随其后的测试窗结果聚合为样本外证据。
- [x] 候选参数在引擎侧去重、稳定排序并限制为最多 24 组；同分训练结果按回撤、交易数和窗口值稳定打破平局，避免候选输入顺序改变结论。
- [x] 返回并持久化优化审计摘要：候选数、训练期选择排序规则、各参数被选中的折数/比例和参数稳定性比例；该摘要不把训练期收益混入样本外指标。
- [x] Walk-forward 端点现与其他回测端点一致支持 `data_version` 及时间范围，使用通过数据质量检查的版本化行情；请求正文随策略版本审计记录保存。
- [x] 增加候选归一化/稳定性、内联与版本化 API、以及无效重复候选的定向测试，并更新 API 文档。
- 兼容性影响：既有响应中的样本外聚合字段保持不变，新增 `result.optimization` 审计对象；重复候选参数现在会在 API 层以 `422` 拒绝，而直接引擎调用仍会安全去重。


## 下一次开工任务

> **第五阶段：策略研究与评估（下一项：滚动窗口回测）**

完成标准：

- [x] 多策略组合回测、参数网格搜索和 Walk-forward 优化已经完成。
- [ ] 增加滚动窗口回测，并清晰区分固定长度滚动训练窗与已实现的 Walk-forward 样本外验证。
- [ ] 补充引擎/API 定向测试、审计记录和 API 文档，且不触发策略参数变更或真实下单。

## 记录规范

每完成一个功能：

1. 勾选本文件中的对应事项
2. 增加对应单元测试
3. 更新 API 或用户文档
4. 运行质量检查
5. 记录兼容性影响
6. 记录下一步任务
